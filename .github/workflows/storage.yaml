name: Storage 💾

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    name: Build 🔨
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23.1'

    - name: Build
      working-directory: repertoire.storage/
      run: go build -v ./...

  unit-testing:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    name: Unit Testing 🧪
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23.1'

    - name: Test
      working-directory: repertoire.storage/
      run: go test -v ./...