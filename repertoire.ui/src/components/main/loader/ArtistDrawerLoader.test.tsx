import { mantineRender } from '../../../test-utils.tsx'
import ArtistDrawerLoader from './ArtistDrawerLoader.tsx'

describe('Artist Drawer Loader', () => {
  it('should render', () => {
    mantineRender(<ArtistDrawerLoader />)
  })
})
