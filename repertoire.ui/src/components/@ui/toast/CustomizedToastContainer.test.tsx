import CustomizedToastContainer from './CustomizedToastContainer.tsx'
import { render } from '@testing-library/react'

describe('Customized Toast Container', () => {
  it('should render', () => {
    render(<CustomizedToastContainer />)
  })
})
