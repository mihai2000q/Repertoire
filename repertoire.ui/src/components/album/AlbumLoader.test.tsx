import { mantineRender } from '../../test-utils.tsx'
import AlbumLoader from './AlbumLoader.tsx'

describe('Album Loader', () => {
  it('should render', () => {
    mantineRender(<AlbumLoader />)
  })
})
