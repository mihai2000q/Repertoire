import { mantineRender } from '../../../test-utils'
import ArtistAlbumsLoader from './ArtistAlbumsLoader'

describe('Artist Albums Loader', () => {
  it('should render', () => {
    mantineRender(<ArtistAlbumsLoader />)
  })
})
