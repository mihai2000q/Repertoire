import { mantineRender } from '../../../../../../test-utils.tsx'
import ArtistLoader from './ArtistLoader.tsx'

describe('Artist Loader', () => {
  it('should render', () => {
    mantineRender(<ArtistLoader />)
  })
})
